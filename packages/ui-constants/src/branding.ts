/** User-facing product name for this fork. */
export const PRODUCT_NAME = "the-best-ccflare";

/** Published npm package and CLI binary name (compatibility). */
export const PACKAGE_NAME = "better-ccflare";

/** Current fork repository (source, issues, releases). */
export const REPOSITORY_URL = "https://github.com/omcdowell/the-best-ccflare";

export const ISSUES_URL = `${REPOSITORY_URL}/issues`;
export const RELEASES_URL = `${REPOSITORY_URL}/releases/latest`;

/** Upstream project this fork builds on. */
export const UPSTREAM_REPOSITORY_URL =
	"https://github.com/tombii/better-ccflare";

/** Original ccflare project. */
export const ORIGINAL_REPOSITORY_URL = "https://github.com/snipeship/ccflare";
